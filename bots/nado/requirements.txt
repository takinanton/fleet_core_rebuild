# nado_bot_v2 requirements
# Pinned to versions confirmed working with old nado_bot (backup 2026-05-24)

# Vertex / Nado Protocol SDK
# Source: old bot exchange_nado.py imports
nado-protocol>=0.1.0

# eth_keyfile (dep of nado-protocol via eth-account) imports pkg_resources;
# setuptools>=82 REMOVED pkg_resources -> ModuleNotFoundError on fresh py3.12 venv
# (hit live on nado-xnn deploy 2026-06-11; old venv had 74.1.3). Pin until eth-keyfile drops it.
setuptools<81

# Data / indicators
pandas>=2.0.0
numpy>=1.24.0

# Env loading
python-dotenv>=1.0.0

# HTTP (used by liquidity.py for exchange API calls)
requests>=2.31.0

# SQLite is stdlib — no extra dep needed
